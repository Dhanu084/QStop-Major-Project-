<link rel="stylesheet" href="/css/other_user.css">


    <div id="basic-info">
        <div id="dp">
            <img src="<%= other_user.avatar %>" alt="<%= other_user.Username%>">
        </div>
        <div id="info">
            <div><%= other_user.Username %> &nbsp;&nbsp;<i class="far fa-user"></i></div>
            <div><%= other_user.designation %>&nbsp;&nbsp;<i class="fas fa-signature"></i></div>
            <div><%= other_user.email %>&nbsp;&nbsp;<i class="fas fa-at"></i></div>
            
            <% if(other_user.id != locals.user.id) {%>
            <div ><a href="/user/toggleFriend/?id=<%= other_user.id %>" id="other-user"><i class="fas fa-user-plus" id="<%= user.id %>"></i></a>
            <%}%>
            <span id="friend-count"><%= friends.length %>&nbsp; friends</span></div>
        </div>
    </div>

    <div id="available-posts">
        <p><h4>Recent posts</h4></p>
        <div id="available-posts-container" class="card"> 
            <%for(let post of posts) {%>
                
                    <div>
                        <a href="/post/single/<%= post.id %>"> <p> <strong><%=post.content%></strong> </p></a>
                    </div>
                
    
            <%}%>
        </div>
    </div>
    
<script src="/js/friendtoggle.js"></script>